---
apiVersion: v1
kind: ConfigMap
metadata:
  name: test-host-aliases
  namespace: default
data:
  test-tumblr1: |
    name: host-aliases
    hostAliases:
      # TODO: add
    env:
      - name: DATACENTER
        value: foo
      - name: FROM_INJECTOR
        value: bar
    containers:
    - name: sidecar-add-vm
      image: nginx:1.12.2
      imagePullPolicy: IfNotPresent
      env:
        - name: DATACENTER
          value: bf2
      ports:
        - containerPort: 80
